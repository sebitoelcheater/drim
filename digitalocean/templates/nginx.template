server {
    listen 80;
    server_name {{host_info.ip}};

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/{{user}}/{{name}};
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/{{user}}/{{name}}/{{name}}.sock;
    }
}
